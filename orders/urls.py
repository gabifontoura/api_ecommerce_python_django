from django.urls import path

# from . import views

urlpatterns = [
    # path('orders/', views.as_view()),
    # path('orders/<int:order_id>/', views.as_view()),
    # path('orders/sold/<int:user_id>/', views.as_view()),
    # path('orders/finished/<int:user_id>/', views.as_view()),
]